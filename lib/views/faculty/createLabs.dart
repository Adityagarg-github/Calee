import 'package:flutter/material.dart';

class createLabs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lab Group")),
      body: Center(child: Text("Lab creation page coming soon!")),
    );
  }
}
